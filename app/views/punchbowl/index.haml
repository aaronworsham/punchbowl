#wrap 
  #main.png_img
    .content
      - semantic_form_for @post, :url => 'facebook/auth' do |form|
        = render :partial => 'message', :locals => {:form => form}
      = image_tag 'facebook.png'
    
