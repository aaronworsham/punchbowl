<%= form_for(@admin_badge) do |f| %>
  <% if @admin_badge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_badge.errors.count, "error") %> prohibited this admin_badge from being saved:</h2>

      <ul>
      <% @admin_badge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
